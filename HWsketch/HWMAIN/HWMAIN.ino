#include "device.h"

// =====================================================
// Drunksafe OLED + button test firmware
// -----------------------------------------------------
// 메인 파일은 전체 흐름만 담당한다.
// 세부 기능은 각 .h / .cpp 파일로 분리되어 있다.
// =====================================================

void setup() {
  setupDevice();
}

void loop() {
  loopDevice();
}
