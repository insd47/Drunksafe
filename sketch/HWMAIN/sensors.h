#ifndef SENSORS_H
#define SENSORS_H

#include "types.h"

// =====================================================
// 센서 측정 함수
// -----------------------------------------------------
// 현재는 실제 센서 대신 랜덤값을 반환한다.
// 나중에 실제 센서 코드가 확정되면 이 파일의 함수 내용만 교체하면 된다.
// =====================================================

// 심박수 측정 함수
// Bpm()은 기기가 켜져 있는 동안 주기적으로 계속 호출된다.
int Bpm();

// 알코올 측정 함수
// alcohol()은 사용자가 입김 측정을 시작했을 때만 호출된다.
float alcohol();

// 현재 상태를 양호/주의/위험 중 하나로 판정한다.
SafetyState judgeSafetyState(float maxAlcoholValue, int currentBpm);

#endif
